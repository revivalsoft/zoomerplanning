{% extends 'base_user.html.twig' %}

{% block title %}Groupes
{% endblock %}

{% block body %}
	<div class="container py-4">
		<h2 class="text-center fw-bold mb-4">📘 Ouvrir un planning de groupe</h2>

		<div class="card shadow-sm">
			<div class="card-body p-0">
				<table class="table table-striped table-hover mb-0" data-toggle="table" data-search="true" data-show-columns="false" data-pagination="true" data-page-size="10">
					<thead class="table-primary">
						<tr>
							<th data-sortable="true">Nom du groupe</th>
							<th class="text-center">Action</th>
						</tr>
					</thead>
					<tbody>
						{% for groupe in groupes %}
							<tr>
								<td class="align-middle">{{ groupe.nom }}</td>
								<td class="text-center">
									<a href="{{ path('app_plannings_public', {'groupe': groupe.id}) }}" class="btn btn-sm btn-outline-success">
										<i class="bi bi-calendar2-week"></i>
										Voir le planning
									</a>
								</td>
							</tr>
						{% else %}
							<tr>
								<td colspan="2" class="text-center text-muted">Aucun groupe trouvé.</td>
							</tr>
						{% endfor %}
					</tbody>
				</table>
			</div>
		</div>
	</div>
{% endblock %}
