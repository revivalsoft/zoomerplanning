<!DOCTYPE html>
<html lang="fr">
	<head>
		<meta charset="UTF-8">
		<title>{{ subject }}</title>
	</head>
	<body style="font-family: Arial, sans-serif; background-color: #f4f4f4; padding: 20px;">
		<div style="max-width: 600px; margin: auto; background-color: #ffffff; padding: 20px; border-radius: 6px; box-shadow: 0 2px 8px rgba(0,0,0,0.1);">
			<h2 style="color: #2c3e50;">{{ subject }}</h2>

			<p style="font-size: 16px; color: #333333;">
				{{ bodyContent|nl2br }}
			</p>

			<p style="margin-top: 30px;">
				<a href="{{ baseUrl }}" style="background-color: #3498db; color: white; text-decoration: none; padding: 12px 24px; border-radius: 4px;">
					Consulter le planning
				</a>
			</p>

			{% if adminMessageUrl is defined %}
				<p style="margin-top: 20px;">
					<a href="{{ adminMessageUrl }}" style="background-color: #6c757d; color: white; text-decoration: none; padding: 12px 24px; border-radius: 4px;">
						Écrire à votre administrateur
					</a>
				</p>
			{% endif %}
		</div>
	</body>
</html>
